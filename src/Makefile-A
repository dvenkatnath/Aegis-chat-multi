# Define the compiler and flags
CC = gcc
#CFLAGS = -O2 -Wall -fPIC 
CFLAGS = -Wall -fPIC #turn off optimization

#Chose below - ONLY ONE
DEBUG = -g -O0 -D_DEBUG -DDOTSO
#RELEASE = -O2
#RELEASE = -g -O0

# Define the object files
OBJS = aes.o Base64.o EncryptionToolsApi.o Hmac.o HighLevED.o Global2.o SHA256.o Functions3.o Pstr.o UberHighLevED.o ProcMSG.o API.o

# Define the shared library
LIB = libCyberKnot.a

all: $(LIB)

$(LIB): $(OBJS)
	#$(CC) -shared $^ -o $@
	ar rcs $(LIB) $(OBJS)

%.o: %.c
# DEBUG
	$(CC) $(CFLAGS) $(DEBUG) -I./include -c $< -o $@
# RELEASE  FINAL VERSIONx1
#	$(CC) $(CFLAGS) $(RELEASE) -I./include -c $< -o $@
# 

clean:
	rm -f *.o
	#rm -f *.a