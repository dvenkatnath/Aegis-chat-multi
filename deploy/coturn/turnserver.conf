listening-port=3478
tls-listening-port=5349
fingerprint
use-auth-secret
static-auth-secret=${TURN_SECRET}
realm=aegis.local
total-quota=100
stale-nonce=600
no-multicast-peers
no-cli
log-file=stdout

# Relay only — clients still try direct P2P first via browser ICE
# WebRTC media ranges (adjust if needed)
min-port=49152
max-port=49252
